package ru.yakovlev05.anal.model.react2d;

/// Реализация в конкретном семестре. В `Link` — общая для пары студентов, в `Node` — пройденная студентом.
public record CommonGroup(int semester, String realization) {
}
